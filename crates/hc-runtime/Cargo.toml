[package]
name = "hc-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
hc-core = { path = "../hc-core" }
tokio = { version = "1", features = ["full"] }
log = "0.4"
futures = "0.3"
async-trait = "0.1"
chrono = "0.4"
