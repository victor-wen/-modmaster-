[package]
name = "hc-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
hc-core = { path = "../hc-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "1"
log = "0.4"

[dev-dependencies]
serde_json = "1"
